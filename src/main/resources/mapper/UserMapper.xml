<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="news.mapper.UserMapper">
    <select id="finduser" parameterType="User" resultType="User">
        select * from user where username=#{username} and password=#{password}
    </select>

    <insert id="addUser" parameterType="User" >
        INSERT INTO user VALUES (null,#{username},#{password},#{gender},#{resume})
    </insert>

    <select id="findusername" parameterType="User" resultType="User">
        select * from user where username = #{username}
    </select>

    <select id="userlist" resultType="User">
        select * from user where username != 'admin'
    </select>

    <!--根据ID删除用户-->
    <delete id="deleteUser" parameterType="string">
        delete from user where id=#{id}
    </delete>

    <!--修改普通用户个人信息-->
    <update id="modiUser" parameterType="user">
        UPDATE user SET username=#{username},password=#{password},gender=#{gender},resume=#{resume} WHERE id=#{id}
    </update>

    <!--根据当前已登录的用户获取该用户的个人信息-->
    <select id="getUserByName" parameterType="string" resultType="user">
        select * from user where username=#{username}
    </select>
</mapper>