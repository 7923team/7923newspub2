<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="news.mapper.NewstypeMapper">
    <!--    查询所有类型-->
    <select id="getAllNewstype" parameterType="string" resultType="Newstype">
        select * from  newstype
    </select>
</mapper>